server:
  port: 8081
  servlet:
    context-path: /home-app

spring:
  application:
    name: home-service
  datasource:
    url: jdbc:mysql://localhost:3306/home_db
    username: admin
    password: password123

# 业务功能开关
features:
  enable-banner: true
  show-recommendations: false

# 下面是工具将会追加代码的地方
# ============================

new_config = true
new_config = true